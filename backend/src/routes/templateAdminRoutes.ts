// This file has been removed as part of StateDocumentTemplate compliance code cleanup.
// The template admin routes were entirely dependent on the StateDocumentTemplate model.

import { Router } from 'express';

const router = Router();

// All template admin routes have been removed.
// StateDocumentTemplate model no longer exists.

export default router;
